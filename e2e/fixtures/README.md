# Playwright Fixtures

Use this folder for shared Playwright setup:

- Valid auth/session helpers
- Seeded route helpers
- Network response factories
- Accessibility and viewport utilities

Fixtures should make authenticated user stories deterministic and should fail loudly when required external credentials are missing.
