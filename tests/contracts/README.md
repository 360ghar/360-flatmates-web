# Web Contract Tests

Use this folder for tests that guard code and API contract drift:

- Route inventory and stale alias checks
- Query key shape and invalidation behavior
- OpenAPI/API response shape checks
- Shared API client edge cases

Each migrated or new test should include the relevant `WEB-ROUTE-###` or `WEB-DATA-###` story ID from `FEATURE_AUDIT.xlsx`.
