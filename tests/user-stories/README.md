# Web User Story Tests

Use this folder for React Testing Library flows that do not need a real browser. Prefer these tests for provider-driven UI behavior, mocked network states, empty/error/loading states, and accessible control behavior.

Name files by domain, for example `app-auth.test.tsx` or `public-discovery.test.tsx`, and include the canonical story ID in each test name.
